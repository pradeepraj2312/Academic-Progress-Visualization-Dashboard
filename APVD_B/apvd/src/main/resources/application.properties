spring.application.name=apvd

# Database URL
spring.datasource.url=${DB_URL:jdbc:mysql://trolley.proxy.rlwy.net:23581/railway?sslMode=REQUIRED}

# MySQL credentials
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:VmfdRsImMMQngYJtCuXCWKOzywCnFdui}

# MySQL Driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

# Server port (Catalyst/AppSail compatible)
server.port=${X_ZOHO_CATALYST_LISTEN_PORT:${PORT:9000}}
# CORS Configuration
spring.web.cors.allowed-origins=http://localhost:5173,http://localhost:3000,https://academic-progress-visualization-das.vercel.app,https://*.vercel.app,https://*.development.catalystappsail.in,https://apvd-50040577853.development.catalystappsail.in,http://localhost:3001
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true
spring.web.cors.max-age=3600

# JWT Configuration
jwt.secret=${JWT_SECRET:MyVerySecureSecretKeyForJWTTokenGenerationAndValidation12345678}
jwt.expiration=${JWT_EXPIRATION:86400000}